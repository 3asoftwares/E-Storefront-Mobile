# 3A Storefront Mobile - Architecture Documentation

This document provides a comprehensive overview of the application architecture, data flow, component structure, and design patterns used in the 3A Storefront Mobile application.

---

## 📋 Table of Contents

1. [High-Level Architecture](#high-level-architecture)
2. [Application Layers](#application-layers)
3. [Navigation Architecture](#navigation-architecture)
4. [State Management Architecture](#state-management-architecture)
5. [Data Flow](#data-flow)
6. [Component Architecture](#component-architecture)
7. [API Architecture](#api-architecture)
8. [Security Architecture](#security-architecture)
9. [Folder Structure](#folder-structure)
10. [Design Patterns](#design-patterns)

---

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           3A STOREFRONT MOBILE APP                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         PRESENTATION LAYER                           │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │   │
│  │  │  Screens │  │   Tabs   │  │  Modals  │  │ Components│            │   │
│  │  │  (app/)  │  │ (tabs/)  │  │ (login)  │  │   (ui/)  │            │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          STATE LAYER                                 │   │
│  │  ┌─────────────────────┐    ┌─────────────────────────────────┐    │   │
│  │  │   CLIENT STATE      │    │       SERVER STATE              │    │   │
│  │  │   (Zustand)         │    │       (React Query)             │    │   │
│  │  │   - Cart            │    │       - Products                │    │   │
│  │  │   - Wishlist        │    │       - Categories              │    │   │
│  │  │   - User Profile    │    │       - Orders                  │    │   │
│  │  │   - Recent Searches │    │       - Addresses               │    │   │
│  │  └─────────────────────┘    └─────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          DATA LAYER                                  │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐ │   │
│  │  │  Apollo Client  │  │  AsyncStorage   │  │   Secure Store      │ │   │
│  │  │  (GraphQL API)  │  │  (Persistence)  │  │   (Credentials)     │ │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
└────────────────────────────────────┼────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          GRAPHQL GATEWAY                                     │
│                    (External Backend Service)                                │
├─────────────────────────────────────────────────────────────────────────────┤
│  Products │ Categories │ Orders │ Users │ Auth │ Reviews │ Addresses        │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Application Layers

### Layer 1: Presentation Layer

The UI layer containing all screens, components, and visual elements.

```
┌─────────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SCREENS (app/)                 COMPONENTS (src/components/)    │
│  ├── (tabs)/                    ├── ui/                         │
│  │   ├── index.tsx (Home)       │   ├── Button.tsx              │
│  │   ├── products.tsx           │   ├── Input.tsx               │
│  │   ├── cart.tsx               │   ├── Card.tsx                │
│  │   ├── wishlist.tsx           │   ├── Badge.tsx               │
│  │   └── profile.tsx            │   ├── Loading.tsx             │
│  ├── product/[id].tsx           │   ├── EmptyState.tsx          │
│  ├── checkout.tsx               │   ├── GradientHeader.tsx      │
│  ├── orders/                    │   └── AnimatedCard.tsx        │
│  ├── login.tsx                  │                               │
│  ├── signup.tsx                 └── products/                   │
│  ├── search.tsx                     ├── ProductCard.tsx         │
│  └── settings.tsx                   └── ProductGrid.tsx         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Layer 2: State Management Layer

Manages application state with clear separation between client and server state.

```
┌─────────────────────────────────────────────────────────────────┐
│                   STATE MANAGEMENT LAYER                         │
├───────────────────────────┬─────────────────────────────────────┤
│     CLIENT STATE          │         SERVER STATE                 │
│     (Zustand Store)       │         (React Query)                │
├───────────────────────────┼─────────────────────────────────────┤
│                           │                                      │
│  cartStore.ts             │  Queries:                            │
│  ├── items[]              │  ├── useProducts()                   │
│  ├── wishlist[]           │  ├── useProduct(id)                  │
│  ├── recentlyViewed[]     │  ├── useCategories()                 │
│  ├── recentSearches[]     │  ├── useOrders()                     │
│  ├── userProfile          │  ├── useAddresses()                  │
│  │                        │  └── useInfiniteProducts()           │
│  Actions:                 │                                      │
│  ├── addToCart()          │  Mutations:                          │
│  ├── removeFromCart()     │  ├── useLogin()                      │
│  ├── updateQuantity()     │  ├── useRegister()                   │
│  ├── toggleWishlist()     │  ├── useCreateOrder()                │
│  ├── addRecentSearch()    │  ├── useAddAddress()                 │
│  └── setUserProfile()     │  └── useValidateCoupon()             │
│                           │                                      │
│  Persistence:             │  Caching:                            │
│  └── AsyncStorage         │  ├── staleTime: 5 min                │
│                           │  ├── gcTime: 30 min                  │
│                           │  └── Automatic refetch               │
│                           │                                      │
└───────────────────────────┴─────────────────────────────────────┘
```

### Layer 3: Data Layer

Handles all data operations including API calls, local storage, and secure storage.

```
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │  Apollo Client  │  │   CacheService  │  │  SecureStorage  │ │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤ │
│  │ GraphQL Queries │  │ Memory Cache    │  │ Auth Tokens     │ │
│  │ GraphQL Mutate  │  │ AsyncStorage    │  │ Credentials     │ │
│  │ Error Handling  │  │ TTL Expiration  │  │ Device ID       │ │
│  │ Auth Headers    │  │ Auto Cleanup    │  │ Biometric Keys  │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Navigation Architecture

### File-Based Routing Structure

```
app/
├── _layout.tsx              ─────────────► Root Layout (Providers)
│                                           ├── ApolloProvider
│                                           ├── QueryClientProvider
│                                           ├── SafeAreaProvider
│                                           └── GestureHandlerRoot
│
├── (tabs)/                  ─────────────► Tab Navigator
│   ├── _layout.tsx                         ├── Home Tab
│   ├── index.tsx                           ├── Shop Tab
│   ├── products.tsx                        ├── Cart Tab (with badge)
│   ├── cart.tsx                            ├── Wishlist Tab (with badge)
│   ├── wishlist.tsx                        └── Profile Tab
│   └── profile.tsx
│
├── product/
│   └── [id].tsx             ─────────────► Dynamic Route (/product/123)
│
├── orders/
│   ├── index.tsx            ─────────────► Order List (/orders)
│   └── [id].tsx             ─────────────► Order Detail (/orders/456)
│
├── login.tsx                ─────────────► Modal Presentation
├── signup.tsx               ─────────────► Modal Presentation
├── forgot-password.tsx      ─────────────► Modal Presentation
├── search.tsx               ─────────────► Modal with Fade Animation
│
├── checkout.tsx             ─────────────► Stack Screen
├── settings.tsx             ─────────────► Stack Screen
├── addresses.tsx            ─────────────► Stack Screen
├── categories.tsx           ─────────────► Stack Screen
└── edit-profile.tsx         ─────────────► Stack Screen
```

### Navigation Flow

```
                                ┌─────────────────┐
                                │   App Launch    │
                                └────────┬────────┘
                                         │
                                         ▼
                                ┌─────────────────┐
                                │  Root Layout    │
                                │  (Providers)    │
                                └────────┬────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
            ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
            │  Tab Navigator │   │    Modals     │   │ Stack Screens │
            │   (tabs/)     │   │  login/signup │   │   checkout    │
            └───────┬───────┘   └───────────────┘   └───────────────┘
                    │
    ┌───────┬───────┼───────┬───────┐
    ▼       ▼       ▼       ▼       ▼
  Home    Shop    Cart  Wishlist Profile
    │       │
    │       └──────────────┐
    │                      │
    ▼                      ▼
┌─────────┐         ┌──────────────┐
│ Product │         │   Search     │
│ Detail  │         │   Results    │
└─────────┘         └──────────────┘
```

---

## State Management Architecture

### Zustand Store Structure

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           ZUSTAND STORE                                  │
│                          (cartStore.ts)                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         STATE                                    │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  items: CartItem[]           # Shopping cart items               │   │
│  │  wishlist: WishlistItem[]    # Saved for later                   │   │
│  │  recentlyViewed: Item[]      # Recently viewed products          │   │
│  │  recentSearches: string[]    # Search history                    │   │
│  │  userProfile: UserProfile    # Current user info                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        ACTIONS                                   │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  Cart:                                                           │   │
│  │  ├── addToCart(item)         # Add item to cart                  │   │
│  │  ├── removeFromCart(id)      # Remove item                       │   │
│  │  ├── updateQuantity(id, qty) # Update quantity                   │   │
│  │  ├── clearCart()             # Empty cart                        │   │
│  │  ├── getTotalItems()         # Get item count                    │   │
│  │  └── getTotalPrice()         # Get cart total                    │   │
│  │                                                                  │   │
│  │  Wishlist:                                                       │   │
│  │  ├── addToWishlist(item)                                         │   │
│  │  ├── removeFromWishlist(id)                                      │   │
│  │  ├── toggleWishlistItem(item)                                    │   │
│  │  └── isInWishlist(id)                                            │   │
│  │                                                                  │   │
│  │  User:                                                           │   │
│  │  ├── setUserProfile(profile)                                     │   │
│  │  ├── loadUserFromStorage()                                       │   │
│  │  └── clearUser()                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      PERSISTENCE                                 │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  Storage: AsyncStorage                                           │   │
│  │  Key: 'cart-storage'                                             │   │
│  │  Serialization: JSON                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### React Query Cache Structure

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        REACT QUERY CACHE                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Query Keys:                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  ['products', page, limit, filters]     ──► Product list         │   │
│  │  ['product', id]                        ──► Single product       │   │
│  │  ['infiniteProducts', limit, filters]   ──► Infinite scroll      │   │
│  │  ['categories']                         ──► Category list        │   │
│  │  ['orders', customerId]                 ──► User orders          │   │
│  │  ['order', orderId]                     ──► Order detail         │   │
│  │  ['addresses', userId]                  ──► User addresses       │   │
│  │  ['me']                                 ──► Current user         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  Cache Configuration:                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  staleTime: 5 minutes    # Data considered fresh                 │   │
│  │  gcTime: 30 minutes      # Cache retention                       │   │
│  │  retry: 1                # Retry failed requests once            │   │
│  │  refetchOnWindowFocus: false                                     │   │
│  │  refetchOnMount: false                                           │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### User Action Flow (Add to Cart Example)

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        ADD TO CART FLOW                                   │
└──────────────────────────────────────────────────────────────────────────┘

User Interaction                State Update                   UI Update
      │                              │                              │
      ▼                              │                              │
┌─────────────┐                      │                              │
│  User taps  │                      │                              │
│ "Add to     │                      │                              │
│  Cart"      │                      │                              │
└──────┬──────┘                      │                              │
       │                             │                              │
       ▼                             ▼                              │
┌─────────────┐              ┌─────────────┐                        │
│ ProductCard │──addToCart──►│  Zustand    │                        │
│  Component  │   (action)   │   Store     │                        │
└─────────────┘              └──────┬──────┘                        │
                                    │                               │
                                    ▼                               │
                             ┌─────────────┐                        │
                             │ Update items│                        │
                             │    array    │                        │
                             └──────┬──────┘                        │
                                    │                               │
              ┌─────────────────────┼───────────────────┐           │
              │                     │                   │           │
              ▼                     ▼                   ▼           ▼
      ┌─────────────┐       ┌─────────────┐     ┌─────────────┐ ┌────────┐
      │ AsyncStorage│       │  Tab Badge  │     │ Cart Screen │ │ Toast/ │
      │  Persist    │       │   Updates   │     │   Updates   │ │Feedback│
      └─────────────┘       └─────────────┘     └─────────────┘ └────────┘
```

### API Request Flow

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         API REQUEST FLOW                                  │
└──────────────────────────────────────────────────────────────────────────┘

Component                 React Query               Apollo Client
    │                          │                          │
    ▼                          │                          │
┌─────────┐                    │                          │
│useProduct│                   │                          │
│  (id)   │                    │                          │
└────┬────┘                    │                          │
     │                         ▼                          │
     │                  ┌─────────────┐                   │
     │   queryKey ────► │ Check Cache │                   │
     │                  └──────┬──────┘                   │
     │                         │                          │
     │            ┌────────────┴────────────┐            │
     │            │                         │            │
     │      Cache Hit                  Cache Miss        │
     │            │                         │            │
     │            ▼                         ▼            │
     │    ┌─────────────┐           ┌─────────────┐     │
     │    │Return cached│           │ Execute     │     │
     │    │   data     │           │ queryFn     │     │
     │    └─────────────┘           └──────┬──────┘     │
     │                                     │            │
     │                                     ▼            │
     │                              ┌─────────────┐     │
     │                              │Apollo Client│     │
     │                              │   .query()  │     │
     │                              └──────┬──────┘     │
     │                                     │            │
     │                              ┌──────┴──────┐     │
     │                              ▼             ▼     │
     │                        ┌─────────┐  ┌─────────┐ │
     │                        │ Auth    │  │ Error   │ │
     │                        │ Link    │  │ Link    │ │
     │                        └────┬────┘  └────┬────┘ │
     │                             │            │      │
     │                             ▼            │      │
     │                        ┌─────────┐       │      │
     │                        │ HTTP    │◄──────┘      │
     │                        │ Link    │              │
     │                        └────┬────┘              │
     │                             │                   │
     │                             ▼                   │
     │                   ┌─────────────────┐          │
     │                   │ GraphQL Gateway │          │
     │                   └────────┬────────┘          │
     │                            │                   │
     │            ┌───────────────┼───────────────┐   │
     │            ▼               ▼               ▼   │
     │     ┌───────────┐   ┌───────────┐   ┌───────────┐
     │     │ Products  │   │  Orders   │   │   Users   │
     │     │ Service   │   │  Service  │   │  Service  │
     │     └───────────┘   └───────────┘   └───────────┘
     │                                                 
     ▼                                                 
┌─────────────┐                                        
│  Component  │◄──────── data, isLoading, error        
│   Render    │                                        
└─────────────┘                                        
```

### Authentication Flow

```
┌──────────────────────────────────────────────────────────────────────────┐
│                       AUTHENTICATION FLOW                                 │
└──────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │   App Start     │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ initializeAuth()│
                    │ Load token from │
                    │  SecureStore    │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
        Token Found                   No Token
              │                             │
              ▼                             ▼
     ┌─────────────────┐          ┌─────────────────┐
     │ Set global token│          │  Guest Mode     │
     │ __AUTH_TOKEN__  │          │  (Limited)      │
     └────────┬────────┘          └────────┬────────┘
              │                            │
              ▼                            │
     ┌─────────────────┐                   │
     │ useCurrentUser()│                   │
     │  Validate token │                   │
     └────────┬────────┘                   │
              │                            │
     ┌────────┴────────┐                   │
     │                 │                   │
   Valid            Invalid                │
     │                 │                   │
     ▼                 ▼                   │
┌─────────┐    ┌─────────────────┐         │
│ Logged  │    │ clearAuthToken()│         │
│   In    │    │ Redirect Login  │◄────────┘
└─────────┘    └─────────────────┘


LOGIN FLOW:
                    ┌─────────────────┐
                    │  Login Screen   │
                    │ email, password │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ useLogin()      │
                    │ mutation        │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ GraphQL Gateway │
                    │ LOGIN_MUTATION  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
          Success                        Failure
              │                             │
              ▼                             ▼
     ┌─────────────────┐          ┌─────────────────┐
     │ accessToken     │          │  Show Error     │
     │ refreshToken    │          │  Message        │
     │ user data       │          └─────────────────┘
     └────────┬────────┘
              │
              ▼
     ┌─────────────────┐
     │ SecureStore     │
     │ Save tokens     │
     └────────┬────────┘
              │
              ▼
     ┌─────────────────┐
     │ Zustand Store   │
     │ setUserProfile  │
     └────────┬────────┘
              │
              ▼
     ┌─────────────────┐
     │ Navigate to     │
     │ Home Screen     │
     └─────────────────┘
```

---

## Component Architecture

### Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       COMPONENT HIERARCHY                                │
└─────────────────────────────────────────────────────────────────────────┘

RootLayout
├── Providers
│   ├── GestureHandlerRootView
│   ├── SafeAreaProvider
│   ├── ApolloProvider
│   └── QueryClientProvider
│
└── Stack.Navigator
    │
    ├── TabsLayout (tabs/_layout.tsx)
    │   │
    │   ├── HomeScreen (tabs/index.tsx)
    │   │   ├── GradientHeader
    │   │   ├── SearchBar
    │   │   ├── CategoryList
    │   │   │   └── CategoryCard[]
    │   │   ├── FeaturedProducts
    │   │   │   └── ProductCard[]
    │   │   └── RecentlyViewed
    │   │       └── ProductCard[]
    │   │
    │   ├── ProductsScreen (tabs/products.tsx)
    │   │   ├── FilterBar
    │   │   ├── SortOptions
    │   │   └── ProductGrid
    │   │       └── ProductCard[]
    │   │
    │   ├── CartScreen (tabs/cart.tsx)
    │   │   ├── CartItemList
    │   │   │   └── CartItem[]
    │   │   ├── CouponInput
    │   │   ├── OrderSummary
    │   │   └── CheckoutButton
    │   │
    │   ├── WishlistScreen (tabs/wishlist.tsx)
    │   │   └── WishlistGrid
    │   │       └── ProductCard[]
    │   │
    │   └── ProfileScreen (tabs/profile.tsx)
    │       ├── ProfileHeader
    │       ├── MenuList
    │       └── LogoutButton
    │
    ├── ProductDetailScreen (product/[id].tsx)
    │   ├── ImageGallery
    │   ├── ProductInfo
    │   ├── VariantSelector
    │   ├── QuantitySelector
    │   ├── AddToCartButton
    │   └── ReviewList
    │
    ├── CheckoutScreen (checkout.tsx)
    │   ├── StepIndicator
    │   ├── AddressSelector
    │   ├── DeliveryOptions
    │   ├── PaymentMethods
    │   ├── OrderSummary
    │   └── PlaceOrderButton
    │
    └── Modals
        ├── LoginScreen
        ├── SignupScreen
        ├── SearchScreen
        └── ForgotPasswordScreen
```

### UI Component Design

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        UI COMPONENTS                                     │
│                      (src/components/ui/)                                │
└─────────────────────────────────────────────────────────────────────────┘

Button
├── Variants: primary | secondary | outline | ghost | danger | accent | soft
├── Sizes: xs | sm | md | lg | xl
├── Features: loading state, disabled, icon support, full-width, rounded
└── Example: <Button title="Add" variant="primary" size="lg" loading />

Input
├── Types: text | email | password | number | phone
├── Features: label, error message, icon, multiline
└── Validation: built-in validation patterns

Card
├── Variants: elevated | outlined | flat
├── Features: header, footer, padding options
└── Usage: product cards, order cards, address cards

Badge
├── Variants: success | warning | error | info | neutral
├── Sizes: sm | md | lg
└── Usage: status indicators, counts, tags

Loading
├── Types: spinner | skeleton | shimmer
├── Sizes: sm | md | lg | full-screen
└── Usage: loading states, placeholders

EmptyState
├── Props: icon, title, description, action
└── Usage: empty cart, no results, no orders
```

---

## API Architecture

### Apollo Client Configuration

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      APOLLO CLIENT SETUP                                 │
│                    (src/lib/apollo/client.ts)                           │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  Apollo Client  │
                    └────────┬────────┘
                             │
                             ▼
              ┌──────────────────────────┐
              │        Link Chain        │
              └──────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  Error Link   │   │   Auth Link   │   │   HTTP Link   │
├───────────────┤   ├───────────────┤   ├───────────────┤
│ Handle errors │   │ Add JWT token │   │ Send request  │
│ Log to Sentry │   │ to headers    │   │ to GraphQL    │
│ Handle 401    │   │               │   │ endpoint      │
└───────────────┘   └───────────────┘   └───────────────┘
                             │
                             ▼
              ┌──────────────────────────┐
              │      InMemoryCache       │
              ├──────────────────────────┤
              │ Normalized caching       │
              │ Type policies            │
              │ Field policies           │
              └──────────────────────────┘
```

### GraphQL Operations

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      GRAPHQL OPERATIONS                                  │
│                    (src/lib/apollo/queries.ts)                          │
└─────────────────────────────────────────────────────────────────────────┘

QUERIES (Read Operations)
├── GET_PRODUCTS_QUERY
│   └── Variables: page, limit, search, category, minPrice, maxPrice, sortBy
├── GET_PRODUCT_QUERY
│   └── Variables: id
├── GET_CATEGORIES_QUERY
│   └── Variables: filter
├── GET_ORDERS_BY_CUSTOMER_QUERY
│   └── Variables: customerId
├── GET_ORDER_QUERY
│   └── Variables: id
└── GET_ME_QUERY
    └── Variables: none (uses auth token)

MUTATIONS (Write Operations)
├── LOGIN_MUTATION
│   └── Input: { email, password }
│   └── Returns: { user, accessToken, refreshToken }
├── REGISTER_MUTATION
│   └── Input: { name, email, password }
├── LOGOUT_MUTATION
├── CREATE_ORDER_MUTATION
│   └── Input: { items, shippingAddress, paymentMethod }
├── CREATE_REVIEW_MUTATION
│   └── Input: { productId, rating, comment }
├── VALIDATE_COUPON_MUTATION
│   └── Input: { code, cartTotal }
├── ADD_ADDRESS_MUTATION
│   └── Input: { street, city, state, zip, country }
├── UPDATE_ADDRESS_MUTATION
│   └── Input: { id, ...fields }
└── DELETE_ADDRESS_MUTATION
    └── Input: { id }
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     SECURITY ARCHITECTURE                                │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        AUTHENTICATION                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Token Storage (SecureStore)          Token Flow                        │
│  ┌───────────────────────────┐       ┌─────────────────────────────┐   │
│  │ iOS: Keychain             │       │ Login ──► Store Token       │   │
│  │ Android: Keystore         │       │      ──► Set Global Token   │   │
│  │                           │       │      ──► All Requests       │   │
│  │ Encryption: Hardware      │       │      ──► Auto Refresh       │   │
│  │ Access: Device Unlock     │       └─────────────────────────────┘   │
│  └───────────────────────────┘                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        INPUT VALIDATION                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  sanitizeInput()              validatePassword()                        │
│  ├── Trim whitespace          ├── Min 8 characters                      │
│  ├── Remove <> (XSS)          ├── Uppercase required                    │
│  └── Length limit (1000)      ├── Lowercase required                    │
│                               ├── Number required                       │
│  isValidEmail()               ├── Special char required                 │
│  └── Regex validation         └── Returns: { isValid, errors, strength }│
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        API SECURITY                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Secure Headers                   Rate Limiting                         │
│  ┌───────────────────────────┐   ┌─────────────────────────────────┐   │
│  │ Authorization: Bearer xxx │   │ Max requests: 100/minute        │   │
│  │ X-App-Version: 1.0.0      │   │ Window: 60 seconds              │   │
│  │ X-Platform: mobile        │   │ Per-endpoint tracking           │   │
│  │ X-Request-ID: uuid        │   └─────────────────────────────────┘   │
│  └───────────────────────────┘                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Folder Structure

```
E-Storefront-Mobile/
│
├── app/                          # SCREENS (Expo Router)
│   ├── _layout.tsx               # Root layout with providers
│   ├── (tabs)/                   # Tab navigator screens
│   ├── product/                  # Product detail (dynamic)
│   ├── orders/                   # Order screens
│   └── *.tsx                     # Other screens
│
├── src/                          # SOURCE CODE
│   │
│   ├── components/               # COMPONENTS
│   │   ├── ui/                   # Reusable UI components
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   └── ...
│   │   └── products/             # Product-specific components
│   │       ├── ProductCard.tsx
│   │       └── ProductGrid.tsx
│   │
│   ├── lib/                      # LIBRARIES
│   │   ├── apollo/               # GraphQL setup
│   │   │   ├── client.ts         # Apollo Client config
│   │   │   ├── queries.ts        # GraphQL operations
│   │   │   └── secureClient.ts   # Enhanced secure client
│   │   └── hooks/                # Custom hooks
│   │       └── index.ts          # React Query hooks
│   │
│   ├── store/                    # STATE MANAGEMENT
│   │   └── cartStore.ts          # Zustand store
│   │
│   ├── config/                   # CONFIGURATION
│   │   ├── env.ts                # Environment variables
│   │   ├── analytics.ts          # Analytics service
│   │   ├── performance.ts        # Performance monitoring
│   │   └── sentry.ts             # Error tracking
│   │
│   ├── constants/                # CONSTANTS
│   │   └── theme.ts              # Design tokens
│   │
│   ├── utils/                    # UTILITIES
│   │   ├── helpers.ts            # General helpers
│   │   ├── cache.ts              # Caching service
│   │   ├── optimization.ts       # Performance utils
│   │   ├── security.ts           # Security utils
│   │   └── secureStorage.ts      # Secure storage service
│   │
│   └── __tests__/                # TESTS
│       ├── components/
│       ├── store/
│       ├── utils/
│       └── integration/
│
├── assets/                       # ASSETS
│   ├── icon.png
│   ├── splash-icon.png
│   └── adaptive-icon.png
│
├── docs/                         # DOCUMENTATION
│   ├── README.md
│   ├── TECHNOLOGY_STACK.md
│   ├── ARCHITECTURE.md
│   └── JENKINS_CI_CD.md
│
└── [Configuration Files]
    ├── app.json                  # Expo config
    ├── eas.json                  # EAS Build config
    ├── package.json              # Dependencies
    ├── tsconfig.json             # TypeScript config
    ├── jest.config.js            # Jest config
    ├── babel.config.js           # Babel config
    ├── metro.config.js           # Metro bundler config
    └── Jenkinsfile               # CI/CD pipeline
```

---

## Design Patterns

### 1. Container/Presenter Pattern

```
Screen (Container)              Component (Presenter)
┌─────────────────────┐        ┌─────────────────────┐
│ - Fetches data      │        │ - Receives props    │
│ - Handles state     │───────►│ - Renders UI        │
│ - Business logic    │        │ - Fires callbacks   │
│ - Navigation        │        │ - Stateless/minimal │
└─────────────────────┘        └─────────────────────┘

Example:
ProductsScreen (Container)      ProductCard (Presenter)
├── useProducts() hook          ├── Receives: product prop
├── Filter/sort state           ├── Renders: image, name, price
├── Navigation logic            ├── Fires: onPress, onWishlist
└── Renders ProductGrid         └── No data fetching
```

### 2. Custom Hooks Pattern

```typescript
// Encapsulate data fetching logic
function useProduct(id: string) {
  return useQuery({
    queryKey: ['product', id],
    queryFn: () => fetchProduct(id),
    enabled: !!id,
  });
}

// Usage in component
function ProductScreen({ id }) {
  const { data, isLoading, error } = useProduct(id);
  // ...
}
```

### 3. Compound Components

```typescript
// Tab bar with badge indicators
<Tabs.Screen
  name="cart"
  options={{
    tabBarIcon: ({ color }) => (
      <TabIcon name="cart" color={color}>
        <CartBadge />  {/* Compound child */}
      </TabIcon>
    ),
  }}
/>
```

### 4. Provider Pattern

```typescript
// Root layout wraps app with providers
export default function RootLayout() {
  return (
    <GestureHandlerRootView>
      <SafeAreaProvider>
        <ApolloProvider client={apolloClient}>
          <QueryClientProvider client={queryClient}>
            <Stack />
          </QueryClientProvider>
        </ApolloProvider>
      </SafeAreaProvider>
    </GestureHandlerRootView>
  );
}
```

### 5. State Machine Pattern (Checkout)

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Address   │───►│  Delivery   │───►│   Payment   │───►│   Review    │
│   Step 1    │    │   Step 2    │    │   Step 3    │    │   Step 4    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
      │                  │                  │                  │
      ▼                  ▼                  ▼                  ▼
  Validate           Validate           Validate           Submit
  Address            Method             Payment            Order
```

---

*Last updated: January 2026*
